# wild_economy Admin Design and Specifications — SupersededThis document is retained only as historical context.## Current source of truthUse these documents instead:- `docs/wild_economy_admin_design_and_specs_updated.md`- `README.md`- `docs/technical-spec_v1.md`## Why this file is supersededThe older version in this file predates the current shipped-defaults and publish behavior. In particular, it does **not** accurately reflect that:- durable shipped coverage/filing defaults now live in generator inputs, not `exchange-items.yml`- `root-values.yml` now carries both clean economic anchors and admin-editable `layout.groups` filing hints- the classifier consumes `layout.groups` before fallback heuristics- `/shopadmin catalog apply` preserves `generated-category` into the published runtime catalog- runtime `/shop` consumes only the published `exchange-items.yml`## Historical noteKeep this file only if you want a record of the earlier design discussion. Do not use it as an implementation reference.